# AI Usage Report – PHP MVC Project

![Démonstration Vidéo du Site](/C:/Users/Mohamed%20Trigui/.gemini/antigravity/brain/515864f4-f673-4799-a660-0fd6499479cb/cart_demo_1777492718000.webp)

**Project:** E-commerce Nexus (Tunisian Market)  
**Student(s):** Mohamed Trigui  

> **How to use this document**  
> 1. Each time you ask the AI for help, **immediately copy/paste** the exchange into the table.  
> 2. Add your **critical analysis** – what did you change? Why? Did the AI suggest something wrong?  
> 3. Write a **justification** – why did you need AI here? (e.g., “first time using Singleton”, “debugging a foreign key error”)  
> 4. Keep the table in chronological order. One row per *major* assistance (no need for trivial typos).

---

## Reporting Table

| **Prompt** (what you asked) | **AI Response** (code / explanation) | **Critical Analysis & Adjustments** (what you modified/fixed) | **Justification** (why AI was used) |
|-----------------------------|----------------------------------------|---------------------------------------------------------------|--------------------------------------|
| *“Scaffold the core architecture: config/Database.php (Singleton PDO), public/index.php (Router), controllers/BaseController.php. Follow strict MVC, POO, and Vanilla PHP without frameworks.”* | *Provided a `Database` class with a private constructor/clone methods and PDO attributes. Provided an `index.php` with a custom `spl_autoload_register`, `session_start()`, and URL parsing for routing. Provided a `BaseController` with `render()`, `json()`, and `redirect()` methods.* | *1. Verified `PDO::ATTR_EMULATE_PREPARES` is set to `false` for maximum SQL injection protection.<br>2. Added `charset=utf8mb4` to the DSN to handle special characters properly.<br>3. Ensured `session_start()` is called globally in `index.php` to prevent "headers already sent" errors later on.* | *Needed a solid foundational setup for routing and database connections. Ensuring the Singleton pattern is implemented correctly without memory leaks or bypassing security was critical before building the entities.* |
| *“Implement the `Product` Entity and `ProductManager` DAO. Use strict typing, fluent setters in the Entity, and prepared statements with manual hydration in the DAO.”* | *Provided `Product.php` with typed properties and getters/setters returning `self`. Provided `ProductManager.php` which utilizes `Database::getInstance()` in the constructor and includes a `hydrate()` helper method to map database rows to object properties.* | *1. Decided to use a manual `hydrate()` method instead of `PDO::FETCH_CLASS` to retain strict control over how data types (like casting strings to `float` or `int`) are handled from the database array.<br>2. Added fluent interfaces to the setters to make object initialization cleaner in the controllers.* | *Needed to ensure the strict separation of concerns required by the architecture. Using AI helped rapidly generate the boilerplate getters/setters while ensuring the correct PDO implementation patterns for querying the database.* |
| *“Create the `HomeController`, `home.php` View, and `style.css`. Ensure the controller passes database products to the view, and the CSS uses a dark mode/neon aesthetic strictly with Vanilla CSS.”* | *Provided a controller that calls `ProductManager->getAll()`. Provided a semantic HTML view that loops through the product objects. Provided a CSS file utilizing CSS variables for a dark theme and hover effects for the product cards.* | *1. Used `htmlspecialchars()` on all dynamic data in the view to prevent XSS vulnerabilities.<br>2. Used `number_format()` to ensure prices display correctly with two decimal places for TND currency.<br>3. Ensured the CSS path in the `<head>` is relative to the Front Controller (`/projet2/Public/`).* | *Needed to implement a visually striking "High-Ticket" aesthetic without relying on Bootstrap/Tailwind. The AI provided the complex CSS grid and hover micro-animations, while I ensured the data bindings in the view remained secure.* |
| *“Implement the Auth system (User entity, UserManager, and AuthController). Ensure secure sessions, password hashing, and prevent session fixation vulnerabilities.”* | *Provided `User.php` Entity, `UserManager.php` with `findByEmail()` and `create()` methods using prepared statements. Provided `AuthController.php` handling POST login logic with `password_verify()` and secure logout.* | *1. Added `session_regenerate_id(true)` upon successful login to mitigate Session Fixation attacks.<br>2. Used `filter_var()` to sanitize the email input before querying the database.<br>3. Ensured the login error message is generic ("Invalid credentials") to prevent user enumeration attacks.<br>4. Fully destroyed the session cookie in the `logout()` method to ensure complete session termination.* | *Authentication is a critical security layer. Using AI ensured that modern PHP security practices (like `password_verify` and session regeneration) were correctly applied rather than relying on outdated MD5 or insecure session handling.* |
| *“Create an `Uploader` class to handle image uploads securely. Validate MIME types and prevent execution of malicious scripts.”* | *Provided `Config/Uploader.php` utilizing `finfo` for strict MIME type checking, filesize limits, and `sha1_file` combined with `time()` for unique and secure filename generation.* | *1. Rejected `$_FILES['image']['type']` which can be spoofed, opting for the built-in PHP `finfo` class.<br>2. Added an explicit whitelist array of allowed MIME types (`image/jpeg`, `image/png`, `image/webp`).<br>3. Ensured the `uploads` directory is created dynamically if it doesn't exist using `mkdir()`.* | *File uploading is extremely prone to vulnerabilities like path traversal or webshell execution. The AI helped implement an enterprise-grade secure upload mechanism required by the specifications.* |
| *“Create an `AdminController` to manage the CRUD operations for products. Protect the routes using sessions.”* | *Provided `AdminController.php` with methods for `index()`, `create()`, `store()`, `edit()`, `update()`, and `delete()`. Also provided `dashboard.php` and `product_form.php` views.* | *1. Added a strict check in the constructor: `if (!isset($_SESSION['user_id']) \|\| $_SESSION['role'] !== 'admin')` to ensure all CRUD methods are protected globally in the controller.<br>2. Wired the `store()` and `update()` methods to utilize the new `Uploader` class if an image is attached via `enctype="multipart/form-data"`.* | *Required an interface to manage products dynamically instead of using SQL dumps. The AI helped quickly scaffold the boilerplate HTML forms and controller routing while maintaining the required security constraints.* |
| *“Implement a multi-criteria search engine on the homepage that queries the database securely.”* | *Modified `ProductManager.php` to include a `search($keyword)` method using `LIKE %keyword%`. Updated `HomeController` to pass the query, and `home.php` to include a search form.* | *1. Ensured the SQL wildcard `%` is appended securely via prepared statement binding array instead of concatenating it directly in the SQL string, which prevents SQL injection.<br>2. Handled the edge case where the search is empty by falling back to the `getAll()` method.* | *The search feature is a core requirement of the project scope. The AI provided the correct PDO prepared statement pattern for SQL `LIKE` queries which can be tricky to bind securely.* |
| *“Fatal error: Uncaught PDOException: SQLSTATE[42S02]: Base table or view not found: 1146 Table 'ecommerce_db.products' doesn't exist”* | *Explained that the database `ecommerce_db` was successfully created, but the `CREATE TABLE` and `INSERT` sections of the `schema.sql` file were not fully executed in the SQL engine (phpMyAdmin). The AI then manually executed the schema file via the MySQL CLI to populate the database properly.* | *No code was modified in the PHP application. The issue was purely related to the MySQL environment state. I verified the connection via `SHOW TABLES IN ecommerce_db` which confirmed it was empty.* | *Debugging a fatal PDO Exception that caused a white screen on the index route. Required AI to quickly diagnose why a PDO query failed despite a successful database connection.* |
| *“Fatal error: Uncaught PDOException: SQLSTATE[HY093]: Invalid parameter number in ProductManager.php when searching”* | *Identified that the `search()` method reused the `:keyword` named parameter twice in the SQL string (`name LIKE :keyword OR description LIKE :keyword`). The AI updated the query to use distinct parameters (`:keyword1` and `:keyword2`) and bind both.* | *1. Modified the SQL query to `WHERE name LIKE :keyword1 OR description LIKE :keyword2`.<br>2. Updated the `execute()` array to pass both `keyword1` and `keyword2` values.* | *Debugging a strict PDO constraint. Because `PDO::ATTR_EMULATE_PREPARES` is set to `false` for security, PDO native execution does not allow reusing the exact same named placeholder multiple times in a single query.* |
| *“When clicking on Sign In, I get a 404 - Controller Not Found error.”* | *Diagnosed that the Front Controller router (`Public/index.php`) was failing to parse paths like `/projet2/Public/index.php/auth/login`. It incorrectly captured `index.php` as the controller name (`Index.phpController`). The AI updated the router logic to strip `index.php` from the extracted path string.* | *1. Added logic in `index.php`: `if (strpos($path, 'index.php') === 0) { $path = substr($path, 9); }` to clean the URL path correctly.<br>2. Modified the default route check to simply `if ($path === '')`.* | *Debugging a custom PHP routing mechanism. Explains the difficulty of parsing raw `$_SERVER['REQUEST_URI']` strings without relying on `.htaccess` rewrites or a framework.* |
| *“Can you make the add cart works?”* | *Provided `CartController.php` with `index()`, `add()`, `remove()`, and `checkout()` methods. Updated `home.php` to link the "Add to Cart" button to the controller via `$_GET['id']`. Created `cart.php` view.* | *1. Decided to store the cart in `$_SESSION['cart']` as an array of `[product_id => quantity]` to avoid hitting the database on every cart interaction.<br>2. Hooked into `ProductManager->getById()` to dynamically fetch product prices during checkout to prevent tampering with prices via HTML injection.* | *The Shopping Cart is a complex state-management issue in Vanilla PHP. Using AI accelerated the creation of the boilerplate controller logic while ensuring the cart values were securely handled entirely on the server side.* |
